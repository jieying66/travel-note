// ============================================================
// 配置
// ============================================================

/**
 * 高德「Web服务」key。
 * 首次打开时写入本地设置，之后可以在设置里改。
 * 这个 key 只能查地图和地名，不涉及账号或资金。
 */
export const DEFAULT_AMAP_KEY = "10987b5dd07b09c7f8c9b945ee43e738";

/**
 * 照片入库时压到的长边像素。
 * 1920 对做视频足够（1080p 视频用不到更高），
 * 比手机原图（往往 4000+ 像素、四五兆）省十倍空间。
 */
export const PHOTO_MAX_EDGE = 1920;

/** 列表缩略图长边 */
export const THUMB_MAX_EDGE = 420;

/** 发给家人的单文件网页里，照片压到这个尺寸（要嵌进 HTML，不能太大） */
export const SHARE_MAX_EDGE = 1280;
